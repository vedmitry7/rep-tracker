# The Vite proxy forwards /api to http://127.0.0.1:8000 in development.
VITE_API_BASE_URL=/api

# A deliberately local-only identity, used until Telegram initData validation is added.
VITE_DEV_PROVIDER=web-dev
VITE_DEV_EXTERNAL_ID=local-user
VITE_DEV_TIMEZONE=Europe/Moscow
VITE_DEV_LANGUAGE=en
